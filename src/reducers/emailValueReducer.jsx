const emailValue = (state = '', action) => {
    switch (action.type) {
        case 'EMAIL_VALUE': 
            return state; 
        default: 
            return state; 
    }
}

export default emailValue; 